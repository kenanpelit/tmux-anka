//! Per-session lazy restore via a built-in ratatui picker.
//!
//! The TUI lands in v0.5.0; stubbed so the keybinding/popup is wired.

use anyhow::{bail, Result};

pub fn pick() -> Result<()> {
    bail!("the picker is not implemented yet (planned for v0.5.0)");
}
